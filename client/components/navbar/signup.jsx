import React, { useState } from 'react';
import Button from '@material-ui/core/Button';

const Signup = (props) => (
  <Button onClick={(e) => alert('Login Button Clicked!')}>
    Sign Up
  </Button>
);

export default Signup;
